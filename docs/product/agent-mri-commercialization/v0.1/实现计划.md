# Agent-MRI v0.1 实现计划

> 本计划执行已批准的“合同驱动的一条 Mock 纵向闭环”。测试先失败，再做最小实现；真实设备始终 BLOCKED。

## 基线

- 工作树：`C:\Users\Administrator\.codex\worktrees\33be\核磁共振场景化平台`
- 分支：`codex/migrate-verified-mri-runtime`
- 计划起点：`6f7db7122ee5f930acd3d08dfe965b19a052e2b3`
- 已存在且保留：`client/build-ascii/`、`client/build-release-ascii/` 未跟踪构建目录，不纳入提交。
- 现有缓存测试候选：Debug 10/10、Release 10/10，只使用 fake SDK/代理；两份 CMake 缓存源目录指向 `C:\tmp\nmr_ui_33be\client`，因此不作为当前工作树验收证据，最终必须使用新的 ASCII build root。

## Task 1：冻结版本化合同

**文件**

- 新建：`docs/product/agent-mri-commercialization/v0.1/产品合同.md`
- 新建：`docs/product/agent-mri-commercialization/v0.1/验收合同.md`
- 新建：`docs/product/agent-mri-commercialization/v0.1/实现计划.md`
- 新建：`docs/product/agent-mri-commercialization/v0.1/页面合同/01.md` 至 `13.md`

**步骤**

1. 写明定位、非目标、角色、真值、模块、风险门、数据源、结果包和能力阶段。
2. 每页逐项列出进入/离开、数据来源、交互、状态、导航、持久化、审计和可访问性。
3. 用 `rg -n "T[B]D|T[O]DO|待[补]|占[位]"` 自审无未决标记。
4. 用脚本检查 13 个页面文件齐全、验收表阈值类型和阶段值只来自允许集合。
5. 检查 `git diff --check` 和 `git status --short`。
6. 只暂存本 Task 文档并提交：`docs: 冻结MRI Mock闭环产品与验收合同`。

## Task 2：13 页交互审计与死按钮清零

**文件**

- 修改：`client/tests/test_main_window.cpp`
- 按失败事实最小修改：`client/src/app/MainWindow.cpp`
- 按需要修改声明：`client/src/app/MainWindow.h`
- 新建：`interaction-audit.md`
- 新建本轮证据：`docs/product/agent-mri-commercialization/v0.1/evidence/interaction/`

**RED**

1. 为每页所有可见 `QPushButton/QToolButton/QComboBox/QLineEdit/QCheckBox/QRadioButton/QListWidget/QTableWidget` 枚举 objectName、enabled、信号后可见结果。
2. 增加“启用控件必须改变可见状态”“禁用控件必须有原因”“装饰卡不可获得按钮行为”和 L2 五字段边界校验的 Given/When/Then QtTest。
3. 从当前 33be 工作树新鲜配置 TDD 构建目录并先运行：
   `C:\msys64\ucrt64\bin\cmake.exe -S client -B C:\tmp\agent-mri-v01-tdd -G Ninja -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON`
   `C:\msys64\ucrt64\bin\cmake.exe --build C:\tmp\agent-mri-v01-tdd --target test_main_window -j 4`
   `C:\tmp\agent-mri-v01-tdd\test_main_window.exe enabledWorkflowControlContract`
4. 记录至少一个真实失败断点和失败输出。

**GREEN**

1. 只修复被测试证实的死控件；允许动作必须更新页面、状态、数据、进度或提示。
2. 不允许或未实现动作设为 disabled，并设置可见原因/tooltip。
3. 重新运行聚焦测试和 `ctest -R main_window --output-on-failure`。
4. 以本次 Release 运行生成 01–13 截图，逐张打开检查，不调用 SDK。
5. 写 `interaction-audit.md`，旧 `interaction-qa.md` 仅作为历史候选。
6. 检查状态，只暂存本 Task 文件并提交：`fix: 清零十三页可见死按钮`。

## Task 3：确定性 Mock 工作流模型

**文件**

- 新建：`client/src/app/MockWorkflow.h`
- 新建：`client/src/app/MockWorkflow.cpp`
- 新建：`client/tests/test_mock_workflow.cpp`
- 修改：`client/CMakeLists.txt`
- 修改：`client/src/app/MainWindow.h`
- 修改：`client/src/app/MainWindow.cpp`
- 修改：`client/tests/test_main_window.cpp`

**RED**

1. 单元测试覆盖 `DataSourceKind { Mock, HistoricalRaw, LiveBlocked }`，后二者返回具体阻塞原因。
2. 覆盖合法状态转换、非法重复开始、唯一 run ID、冻结 snapshot ID。
3. 覆盖暂停、继续、取消；取消保留证据且图/QC/成功包为空。
4. 覆盖直接打开成功页仍保持 Empty。
5. 运行 `test_mock_workflow` 与聚焦 `test_main_window`，确认新增测试失败。

**GREEN**

1. 实现不依赖 QWidget 的 `MockWorkflow`；用可注入时钟/ID 生成器保证单测确定性。
2. `MainWindow` 只根据模型状态推进页面和展示数据；保留三栏布局；失败/取消时清空图像与 QC。
3. 09 使用模型进度；暂停/继续/取消统一调用模型，不调用 DeviceBridge。
4. 运行聚焦测试和 Debug `ctest -R "mock_workflow|main_window"`。
5. 检查状态，只暂存本 Task 文件并提交：`feat: 实现可验证Mock工作流状态模型`。

## Task 4：Mock 重建、QC、结果包与历史

**文件**

- 新建：`client/src/app/MockResultPackage.h`
- 新建：`client/src/app/MockResultPackage.cpp`
- 新建：`client/tests/test_mock_result_package.cpp`
- 修改：`client/src/app/MockWorkflow.h`
- 修改：`client/src/app/MockWorkflow.cpp`
- 修改：`client/src/app/MainWindow.h`
- 修改：`client/src/app/MainWindow.cpp`
- 修改：`client/tests/test_main_window.cpp`
- 修改：`client/CMakeLists.txt`

**RED**

1. 测试 10 处理前图/QC为空；合法 Mock 资产处理后才有图。
2. 测试 `ImageQualityEvaluator` 的输出被绑定当前 run ID，失败时 QC 为空。
3. 测试七项结果包、manifest 来源/commit/身份/工件路径大小哈希。
4. 预建同名目录后保存必须失败，目录哈希保持不变。
5. 测试历史空目录无样例行；有效 manifest 产生一行；损坏 manifest 标 Error。
6. 测试结果页打开/复制路径、`RetryMockProcessingButton`、`RetryMockQcButton` 和外部分析 disabled 原因。

**GREEN**

1. 复用 `:/mock-reconstruction.png` 与 `ImageQualityEvaluator`；不创建新重建算法。
2. 用 Qt JSON、`QSaveFile`、`QCryptographicHash` 写入非覆盖结果包，不引入依赖。
3. 默认根目录为 Documents 下 `ScenarioNmr\MockRuns`，测试注入临时目录。
4. 13 只读取真实封存 manifest，删除硬编码 Mock-01/Mock-02。
5. 运行 `test_mock_result_package`、`test_mock_workflow`、`test_main_window`。
6. 检查状态，只暂存本 Task 文件并提交：`feat: 封存可追溯Mock结果包与历史记录`。

## Task 5：真实设备接口合同与统一 BLOCKED 门禁

**文件**

- 修改：`client/src/app/MockWorkflow.h`
- 修改：`client/src/app/MockWorkflow.cpp`
- 修改：`client/src/app/MainWindow.cpp`
- 修改：`client/tests/test_mock_workflow.cpp`
- 修改：`client/tests/test_main_window.cpp`

**RED**

1. 测试无参数默认数据源为 Mock。
2. 测试 Live 入口列出 SDK 身份、设备 IDLE、横断位映射、快照、隔离输出、唯一 owner、人工授权七项缺失。
3. 测试普通界面真实动作全部不可达；既有 fake SDK opt-in 测试仍存在。

**GREEN**

1. 统一展示 `LIVE: BLOCKED` 和逐项原因，不新增真实副作用。
2. 不删除 `DeviceBridge/MriSdkLoader` 或既有 fake SDK 测试。
3. 运行聚焦测试与全量 Debug CTest。
4. 检查状态，只暂存本 Task 文件并提交：`test: 固化真实设备BLOCKED门禁`。

## Task 6：本次运行、证据与发布收口

**文件**

- 更新：`interaction-audit.md`
- 新建：`docs/product/agent-mri-commercialization/v0.1/追踪矩阵.md`
- 新建/更新：`docs/product/agent-mri-commercialization/v0.1/evidence/`
- 更新验收结果：`docs/product/agent-mri-commercialization/v0.1/验收合同.md`

**验证**

1. 新鲜配置、构建并运行 Debug：
   `C:\msys64\ucrt64\bin\cmake.exe -S client -B C:\tmp\agent-mri-v01-debug -G Ninja -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON`
   `C:\msys64\ucrt64\bin\cmake.exe --build C:\tmp\agent-mri-v01-debug -j 4`
   `C:\msys64\ucrt64\bin\ctest.exe --test-dir C:\tmp\agent-mri-v01-debug -C Debug --output-on-failure`
2. 新鲜配置、构建并运行 Release：
   `C:\msys64\ucrt64\bin\cmake.exe -S client -B C:\tmp\agent-mri-v01-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON`
   `C:\msys64\ucrt64\bin\cmake.exe --build C:\tmp\agent-mri-v01-release -j 4`
   `C:\msys64\ucrt64\bin\ctest.exe --test-dir C:\tmp\agent-mri-v01-release -C Release --output-on-failure`
3. 用精确命令部署：
   `powershell -ExecutionPolicy Bypass -File client\scripts\deploy.ps1 -BuildRoot C:\tmp\agent-mri-v01-release -DistRoot C:\tmp\agent-mri-v01-release\dist-full-flow-ui-v2`
4. 无 SDK/auto-connect 参数启动唯一最终 EXE；记录 PID、启动时间、路径、大小、SHA-256 和模块。
5. 从本次可见运行点击完成一条 Mock 路径，封存结果包；校验 manifest 与所有工件哈希。
6. 本次运行逐页截图 01–13，并逐张可视检查。
7. 更新验收合同和追踪矩阵；真实设备保持 BLOCKED。
8. 若所有 v0.1 门禁通过，结论为 `GO`；Mock 可用但存在不影响主闭环的发布限制为 `CONDITIONAL GO`；任一 P0 或来源/安全门失败为 `NO-GO`。
9. 检查 `git diff --check`，只暂存证据与报告并提交：`docs: 记录Mock闭环发布验收证据`。

## 完成判据

- 合同、代码、测试、截图、manifest、哈希和运行进程身份相互一致。
- 01–13 启用控件无死按钮。
- Empty/取消/失败状态无图像和 QC。
- 成功结果包可追溯且不覆盖。
- 默认可见流程对真实设备所有副作用为 0，Live 明确 `BLOCKED`。
- 最终报告只依据本次新鲜证据，不沿用历史“能启动”结论。
