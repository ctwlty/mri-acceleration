# Project A｜05 测试证据与已知缺口

本页只读取 `80b5d85` 提交树及该提交可见的历史报告。

## 自动化测试现状

**VERIFIED FACT**：`80b5d85` 的 Qt `client` 没有 `client/tests`、CTest、fake DLL 或自动化断言；`client/CMakeLists.txt` 只构建 GUI。

同一仓库快照另有 `testDLL/Program.cs`、`testDLL/test.cpp`、`testDLL/testDLL.csproj` 以及 `eggcontrollerV2/Iface/console_mock.py`。它们是 legacy 手工 SDK exerciser/模拟实现，不是 Qt client 的自动测试套件，也没有本轮执行证据。因此本文不把它们计为 client CTest，但也不把整个仓库误写为“没有任何测试源”。

| 历史证据 | 它最多证明什么 | 不能证明什么 |
|---|---|---|
| `.harness/changes/2026-07-19-v2-client-implementation/test-report.md` | 文档声称 macOS Qt configure/build 成功 | 当前 Windows 可构建；真实 SDK 可用 |
| `.harness/changes/2026-07-20-compact-template-selector/test-report.md` | 文档声称 macOS offscreen 启动后由 timeout/人工停止 | 完整交互、可见布局、无死按钮 |
| `.harness/changes/2026-07-19-v2-client-implementation/test-report.md` | 明确写 Windows real SDK 跳过 | 任何真实 Init/Run/Abort |
| `.harness/changes/2026-07-20-sdk-dry-run-mapper/review.md` | 有 build/startup 检查 | 用户实际点击并校验 DRY_RUN 内容 |
| `.harness/changes/2026-07-21-local-sdk-no-git/test-report.md` | ignore 规则匹配预期 DLL 路径 | DLL 曾被加载或 ABI 正确 |

**INFERENCE（中置信）**：因为 `b1fe81a` 到 `80b5d85` 的 Qt 源码/CMake Git 对象相同，上述报告很可能针对同一源码；但报告缺少 commit SHA、二进制哈希和完整日志，不能升级为当前可复现证据。

## 未覆盖矩阵

| 范围 | 缺失证据 |
|---|---|
| 构建/发行 | Windows configure/build、windeployqt、安装/回滚、依赖清单 |
| UI | 按钮 enabled/反馈、三级筛选、空搜索、Resume 不可达、DPI、辅助功能 |
| DLL | 导出绑定、ABI、Init 顺序、部分失败 rollback、reload、CloseSys |
| 设备 | 连接码、温度、空闲、Run、状态轮询、完成、Abort acknowledgement |
| 参数 | UI 模板与实际 SDK setter 的一致性、参数文件存在/身份 |
| 文件 | DRY_RUN 内容/权限/重复命名；RAW 新增判定 |
| 工作流 | 预检门禁、run owner、timeout/cancel/retry/recovery |
| 成像/结果 | RAW、重建图、QC、结果包、manifest、历史 |

## As-Is 证据结论

- **VERIFIED FACT**：Project A 是默认 Demo 的单窗口 Qt 控制台；库存 Real Run 全 HOLD，但真实 Load/Init/Abort 代码可达。
- **VERIFIED FACT**：没有自动化测试证明这些接口或交互正确。
- **UNKNOWN**：A 在任何目标 Windows + 真实设备组合上的完整运行状态；本轮严格未执行。
