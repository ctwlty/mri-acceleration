# 92 — 离线身份与接口证据

> 采集日期：2026-08-02（Asia/Shanghai）。本文件只记录离线、只读事实；只用离线脚本读取字节和计算哈希，没有启动 eggcontroller/Qt/SDK/设备，没有 `LoadLibrary`/`ctypes.CDLL` 实例化，没有调用 `Run`、`Abort`，也没有复制 DLL、RAW 或运行数据。

## 范围与方法

- 工作树基线：分支 `codex/migrate-verified-mri-runtime`，审计开始 HEAD `bd7e28b46828107827aef4b68e3614873df91d48`。采集时并发的表征测试任务正在修改 `client/tests/test_main_window.cpp`，另有两个既存未跟踪 `client/build-*-ascii/` 目录；本身份任务未触碰这些范围。
- DLL 使用字节级 PE 解析（另以 `objdump -p` 作静态交叉检查）；**未**把该文件传给 Windows loader。
- Desktop 目录清单只读取文件名、大小和 SHA-256；结果在 [evidence-stage2](evidence-stage2) 中。JSONL 中仅保留元数据，不复制任何被散列文件。
- 厂家 C/C++ 头文件、厂商 ABI/调用约定说明和签名版 API 文档均未提供。因此任何从导出名、C#、Python 或 PDF 文本抽取推定的 ABI 都不是厂家权威证据。

## 1. `mridll.dll` 的离线身份与 PE 表

目标绝对路径：`C:\Users\Administrator\Desktop\eggcontrol\eggcontrollerV2\Iface\mriRely\mridll.dll`

| 字段 | 离线事实 |
| --- | --- |
| size | 34,896,384 bytes |
| SHA-256 | `D32AF2B676A4956A3D9AB8707B49F47083328A5CE9236FBB5324E44C28054CE8` |
| PE | `PE32+`、machine `0x8664`、AMD64/x64、8 sections |
| 导入 | 15 个 DLL：`KERNEL32.dll`、`MSVCP140.dll`、`IPHLPAPI.DLL`、`WS2_32.dll`、`VCRUNTIME140.dll` 和 10 个 `api-ms-win-crt-*` DLL；逐模块符号数和完整导出名见 JSON |
| 导出 | 328 个具名导出，包括 `Init`、`ConfigFile`、`Run`、`Abort`、`ScanStatus`、`CloseSys`、`SetPreempValue`、`C_SetPreempValue` |

可机器复核的完整静态记录：[mridll-static-pe.json](evidence-stage2/mridll-static-pe.json)。该文件的 `staticMethod` 明确为 byte parsing；PE 导出表只证明名称/ordinal/RVA 存在，**不**编码函数参数、返回类型或调用约定。

## 2. `SetPreempValue` 声明比较

| 位置/版本 | 声明或状态 | 返回类型 | 调用约定 | 判定 |
| --- | --- | --- | --- | --- |
| A `80b5d85` Qt loader | 未绑定 `SetPreempValue` | 不适用 | 不适用 | 不能作为签名来源 |
| Qt `c236e00` | `using SetPreempValueFunc = int (*)(int, int, float);` | `int` | 源码未标注 `__cdecl`/`WINAPI` | 声明性证据，非厂家证据 |
| Qt HEAD | 与 `c236e00` 同一 blob：`.h` `2e7dc20…`，`.cpp` `d01cc9b…`；`prepareScan()` 将结果交给 `checked()` | `int` | 未显式标注 | 同上 |
| C# `NMRDLL/NMRDLL.cs` | `[DllImport("mridll.dll")] public static extern void SetPreempValue(shimChannel channel, int keys, float value);` | `void` | 未显式设置；`DllImport` 默认 `Winapi` | 与 Qt 返回类型冲突 |
| 仓内历史 Python（`8e4ad0b`/HEAD 的 `eggcontrollerV2/Iface/consolev3.py`） | `CDLL`，但没有该函数的 `argtypes/restype` 声明 | UNKNOWN | `CDLL` 使用默认 ctypes 约定；未显式声明 | 不能补足签名 |
| Desktop 当前 Python | `CDLL`；`argtypes=[c_int,c_int,c_float]`，`restype=c_int` | `c_int` | `CDLL`，未显式注明厂家调用约定 | 与 Qt 返回类型一致，但仍非厂家证据 |
| `pdf_text.txt` 的文本抽取 | 出现 `int SetPreempValue(int channel, int keys, float value)` 片段 | 声称 `int` | UNKNOWN | 非原始头文件/无来源链，不能提升为权威 |

结论：参数形状 `(int, int, float)` 在 Qt 与 Desktop Python 中一致；返回类型至少存在 **`int`（Qt/Python）与 `void`（C#）的 CONTRADICTED 声明**。PE 名称表不能消解此冲突。厂家头文件缺失时，目标 DLL 的完整函数 ABI、返回码语义和调用约定均为 **UNKNOWN**；不得据此运行或修改真实调用链。

相关源文件 SHA-256：Qt `MriSdkLoader.h` `055F6D88F28C0A93CA99C1F02BE2F26DF07752CCC668B7496EB8BD69C6F91DD5`，Qt `MriSdkLoader.cpp` `705A41F83A5E69E9C78B3BCAC884DC7909BB7BE761E710E43331E5D03B899E3F`，C# `NMRDLL.cs` `D14D485A9FBE37CA9F8A9F2F4E44CCB57BCD8F786187B94615C64F6FE71B7C72`，Desktop `consolev3.py` `D98F4BC8F36583B290F35255DD7D4BBCAFBC448A9F263103E0B96DBD500334E5`。

## 3. Desktop `eggcontrollerV2` 目录与接口事实

根目录：`C:\Users\Administrator\Desktop\eggcontrol\eggcontrollerV2`；根目录不存在 `.git`，故是可变的、**非 Git** 外部目录。

[desktop-eggcontrollerV2-manifest.json](evidence-stage2/desktop-eggcontrollerV2-manifest.json) 描述采集方法、根目录、排除规则和计数；[desktop-eggcontrollerV2-files.jsonl](evidence-stage2/desktop-eggcontrollerV2-files.jsonl) 有 644 条 `{relativePath,size,sha256}`。验证结果：644 条、90,298,866 bytes、零重复路径。

排除仅限缓存/日志/生成结果：`.pytest_cache/**`、任意 `__pycache__`、`log_data/**`、`gui_debug.log`、`webdata/data/**`、Temp wheel 缓存，以及潜在 RAW/运行结果 `request/kspace.fid`。最后一项没有被散列或复制。没有以 MIME、扩展名或文件大小作其他泛化排除。

关键外部文件的只读哈希：

| 文件 | size | SHA-256 | 观察 |
| --- | ---: | --- | --- |
| `MainWindow_V3.py` | 49,895 | `4340D339AEEF9FFDCE3A4B63218BE7564F37CA71D80A55E07197E77FD98852A2` | 有 `demoCheckbox`；`samplingBtn_click_sync()` 读取它 |
| `Iface/HCController.py` | 1,932 | `70DE8B87C54BE24CB0D8A55F5652A9D6A1B10838985829FA083549F549C5DD5F` | 实际 `from Iface.consolev3 import console` |
| `Iface/consolev3.py` | 11,924 | `D98F4BC8F36583B290F35255DD7D4BBCAFBC448A9F263103E0B96DBD500334E5` | 构造器含 `CDLL`；本阶段未实例化 |

与仓内历史/当前 proxy 的契约核对：

- 仓内 `client/tools/eggcontroller_proxy.py::run_once` 注释与代码都要求 `ui.demoCheckbox.setChecked(False)`，并恰好调用一次 `samplingBtn_click_sync()`；其 SHA-256 为 `9BB8A82F72BA7FF2036D02043A905B90478E4DD61089836964B2F5A1A5FCF6F5`。
- Desktop 当前 UI 确有 `demoCheckbox`（构建代码约 938–941 行）和同步入口（约 484 行），故这两个属性在该外部快照中成立；这不等于已经执行 proxy 或采集。
- 仓内 `eggcontrollerV2/Iface/HCController.py`（SHA-256 `71BB939A3C2D9CAD7BC09C5C17A5B34CDA16CDF81569278C89A10777B00116C6`）仍实际 import `console_mock`，`consolev3` 是注释行；这与 Desktop 当前 `consolev3` import **CONTRADICTED**。
- Desktop `consolev3.py` 文本含 1,500 秒超时、异常状态 `-1/5/6` 时的 `Abort()`，以及清理中的 `Abort()`/`CloseSys()`；Desktop `MainWindow_V3.py` 另有自动循环 300 秒超时。它们是离线源码事实，不是被本阶段调用/验证的安全语义。
- 仓内旧 UI 亦有 `samplingBtn_click_sync()` 和一个图像等待计数器（100 × 0.01 秒），但没有 `demoCheckbox`；因此 proxy 的 fake UI 测试不能证明仓内旧 UI 或 Desktop 外部目录的实机链。

## 4. c77 固定清单与当前外部资产的比较

c77 的 `client/runtime/mri-runtime-manifest.json` 固定了以下历史生产基线；当前 HEAD 不存在该 manifest、`stage-mri-runtime.ps1` 或 `MriRuntimeResolver.*`。`c77aeb4:.gitignore` 还忽略 `client/dist/`、`**/mridll.dll`、`**/NMRDLL.dll` 和 runtime/generated scan data，故其 `dist` 不是可用于当前工作树身份判断的受版本控制资产。

| 资产 | c77 固定值 | 当前只读观测 | 结论 |
| --- | --- | --- | --- |
| `mridll.dll` | SHA-256 `D32AF2…054CE8` | 34,896,384 bytes，完全相同 SHA-256 | MATCH（仅二进制身份） |
| `hw_cfg` | 455 files、206,656 bytes、目录 hash `A8BFF7…C45F6F` | 同一计数/字节数/目录 hash | MATCH |
| `hw_cfg/init.ini` | `644D2F…411956` | 301 bytes，相同 SHA-256 | MATCH |
| `C:\MRIScanner\Scan\PTScan.par` | 21,684 bytes，`6FD62B…36783` | 1,684 bytes，`20859C049888A479E23B18C4EA48E80A6753722F917458CCFE98EA09F57FF6C4` | **CONTRADICTED** |
| Desktop `Iface/mriRely/PTScan.par` | c77 的参数文件名/哈希 | 1,853 bytes，`0DF4F238B7EBEB7A5DB9E3F0E3136AEF1FA286897C1F79D5E383F160F0AD4EE6` | **CONTRADICTED** |
| fcode / `:SRC` | c77 manifest 不包含 fcode 身份条目 | 两个当前 PTScan 的首行均指向 `C:\MRIScanner\FTSpect\seq_lib\demo_src\CSJ_1PULSE_R1.src`；该 `.src` 不存在。同目录同 stem 的 `CSJ_1PULSE_R1.fcode` 存在，55,988 bytes，SHA-256 `3264E171…E5948` | **UNKNOWN**：同 stem 只是文件名关联，不能证明任一 PTScan 实际加载了该 fcode |

`hw_cfg` 目录 hash 按 c77 `Get-DirectoryManifestSha256` 的 UTF-8 `relativePath|size|sha256` 排序记录算法计算；因此上表的 MATCH 是可重复的 manifest 比较，并不证明设备/厂商认可其为现行权威。两个 `PTScan.par` 均与历史 c77 值漂移，且彼此也不相同；本阶段不猜测哪一个有权威性。fcode 的完整静态记录见 [fcode-static-evidence.json](evidence-stage2/fcode-static-evidence.json)；`C:\MRIScanner` 内另有多份 `.fcode`，但没有不可变清单或调用日志把其中任一文件绑定到当前参数、代码或一次运行。

## 未决项（必须保持 UNKNOWN）

1. 厂家授权的 `mridll.dll` C/C++ 头文件、精确 ABI/调用约定、返回码和 `SetPreempValue` 返回类型。
2. 外部 Desktop 目录、DLL/hw_cfg 当前身份与某一允许的实机配置之间的授权/变更控制关系。
3. 两个漂移的 PTScan 文件、fcode 资产和设备实际加载资产的权威来源与语义。
4. `Abort` 在部分初始化、超时、异常、清理和设备断连时是否安全、幂等并可恢复。

这些结论要求厂家材料与另行明确授权的、分阶段的验证协议；不由本离线证据推导。
