# Project B｜05 测试证据与已知缺口

本页区分“测试源码存在”和“本轮新鲜运行通过”。本轮没有执行 CTest，因此不能把任何里程碑写成当前全绿。

## 1. 各里程碑注册测试

| 快照 | 注册数 | 覆盖面 | 明确限制 |
|---|---:|---|---|
| `c236e00` | 6 | loader、bridge、device actions、MainWindow fake auto-connect、verifier fake、GUI auto-connect smoke | 只用 fake SDK；MainWindow 测试只覆盖 public auto-connect |
| `c77aeb4` | 8 | 増 runtime resolver 与 stage script | 只属于 runtime 分支；无本轮运行证据 |
| `8e4ad0b` | 9 | 增 EggControllerProcess、Python proxy、automation GUI fake | fake proxy/FakeUi；不证明真实 eggcontroller/设备 |
| `4207800` | 10 | 增图像 QC 与 13 页/HOLD 断言 | 生产 HOLD 与旧 automation 测试静态矛盾 |
| `91343be` | 12 | 增 MockWorkflow、MockResultPackage、扩展 MainWindow、QC、SDK/proxy smoke | 两组关键 UI/automation 覆盖被禁用/提前返回 |

## 2. 当前测试源码可证明什么

若在对应提交与受控环境中通过：

- `test_mri_sdk_loader`：fake DLL 导出绑定、错误传播、调用序列。
- `test_device_bridge` / `test_device_actions`：会话状态、fake 扫描轮询和 UI availability 规则。
- `test_mock_workflow.cpp:98-306`：数据源阻塞、准备门禁、run/snapshot 冻结、暂停/取消、失败和 QC hash 约束。
- `test_mock_result_package.cpp:107-329`：7 文件、同名拒绝、hash/身份交叉校验、损坏历史。
- `test_main_window.cpp:2001-2203`：测试进程内 01→13、实际写 Mock 包、manifest/历史、设备保持 Unloaded。
- `test_main_window.cpp:2298-2344`：取消不生成成功工件。
- CLI smoke：fake auto-connect/idle/automation 配置时进程行为。

以上“可证明”仍以测试实际执行通过为前提；本轮只确认源代码存在。

## 3. 测试内部缺口

- **VERIFIED FACT**：`91343be:client/tests/test_main_window.cpp:207-378` 的 automation GUI 测试整体位于 `#if 0`，不参与执行。
- **VERIFIED FACT**：`visibleWorkflowControlsHaveNamesAndObservableBehavior()` 在约 `:672` 无条件 `return`；其后约 `:674-852` 的控件行为断言不可达。
- **VERIFIED FACT**：`gui_automation_mode_fake` 只证明带 automation 参数的 GUI 保持 idle，不证明代理 start、真实 UI import 或工件回传。
- **VERIFIED FACT**：`4207800` 测试仍要求 Start enabled/返回两图，而生产配置将 Start disabled 且 `handleStart` HOLD 返回；这是静态矛盾，不能称 420 全绿。

## 4. 当前不能证明的内容

| 类别 | 未证明 |
|---|---|
| 构建/发行 | 当前 HEAD Debug/Release 全绿、发布目录对应 commit、唯一二进制身份 |
| UI | 所有 enabled 控件有反馈、目标 DPI、可见运行、键盘/辅助功能、13页无断路 |
| Mock 状态 | Running/Paused 关窗、崩溃恢复、取消/失败审计持久化、失败阶段合法重试 |
| 文件系统 | 磁盘满、并发写、rename/cleanup 失败、大历史性能、跨进程 run ID 冲突 |
| eggcontroller | 提交内真实 UI import、非 mock 输入、真实 DLL/设备、timeout/cancel |
| runtime | 当前 `33be` 的 resolver/manifest/identity；代码根本不存在 |
| 实机 | 真实 DLL ABI、设备连接、温度/idle、Run/Abort、RAW 与 run/snapshot/hash 绑定 |

## 5. 证据等级结论

- **VERIFIED FACT**：B 的测试面从 0 扩到 12 个注册测试，且形成可独立单测的 Mock 状态和结果包模型。
- **VERIFIED FACT**：测试数量不能消除禁用测试、提前返回和分支未合并带来的覆盖空洞。
- **INFERENCE（高置信）**：当前测试对“确定性 Mock 核心”的证据强于对“完整 UI 交互”和“任何真实外部链”的证据。
- **UNKNOWN**：当前主机最新代码的实际通过数；本轮按边界未构建/运行。
