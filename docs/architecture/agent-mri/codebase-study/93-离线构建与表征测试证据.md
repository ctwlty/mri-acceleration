# 93 离线构建与表征测试证据

日期：2026-08-02（Asia/Shanghai）

## 1. 范围与源码身份

- 工作树：`C:\Users\Administrator\.codex\worktrees\33be\核磁共振场景化平台`
- 分支：`codex/migrate-verified-mri-runtime`
- 第一阶段安全审查基线：`bd7e28b46828107827aef4b68e3614873df91d48`
- 首轮表征测试：`7aa2fc84cc4b0a28316d0c6fc6ae5e44ce11874e`
- 最终隔离修正及本页 current 构建身份：`70c5607454d661a9b325798b627c1412ed2869ba`
- 生产源码、构建脚本和设备适配相对 `91343be` 零差异：`git diff --quiet 91343be..70c5607 -- client ':(exclude)client/tests/**'` 返回 0。
- 既有未跟踪 `client/build-ascii/`、`client/build-release-ascii/` 未删除，也未被当作本阶段构建或证据目录。

最终关键文件 SHA-256：

| 文件 | SHA-256 |
|---|---|
| `client/CMakeLists.txt` | `3F79B4A5ABE5211237170C1B78B8A0C62D6816FDCB4A71BD318CE1061AA46F83` |
| `client/tests/test_main_window.cpp` | `5C04CBD81871F998AD733A943563016F10762169E2C923937D605D91621B7133` |
| `client/tests/fake_mri_sdk.cpp` | `D8C750E2733C2FBACB0EEEB8B6610FA0824063A1E1534F2EA66F719C7ECF1A9E` |
| `client/tests/fake_incomplete_mri_sdk.cpp` | `34343404E6F11DD23A23B9B9CB7599DADC77CB83BC66D858AEA2381B122D8B9F` |
| `client/tests/fake_eggcontroller_proxy.cpp` | `AEFF29C600EB363EAC3C588F5053071787CC96FFD2A5D5E67512B74E3EB9B8E0` |
| `client/tests/run_gui_auto_connect_test.cmake` | `67124365BB111C69F1BDCE6E7752B7B5A5E49862D69B6ED8C326BEB09B9C2685` |
| `client/tests/run_gui_automation_mode_test.cmake` | `CD586A622AADF7D6F182C3CD884602C8E04E775C6C25C95CCB8B1C4845B9BD1E` |
| `client/tests/run_sdk_verify_test.cmake` | `2F2B9A1268FCF6043C17B81F00D56EC5637D8E738C1EF730A3B73830380B2A48` |
| `client/tools/eggcontroller_proxy.py` | `9BB8A82F72BA7FF2036D02043A905B90478E4DD61089836964B2F5A1A5FCF6F5` |

## 2. 12 项 CTest 副作用审查

| CTest | 源码实际行为 | 本阶段裁决 |
|---|---|---|
| `mri_sdk_loader` | 由 CTest 注入构建目录中的完整/不完整 fake DLL；会 `LoadLibrary`，但不会读取真实 D32。 | SAFE-FAKE；运行。 |
| `device_bridge` | fake DLL 内模拟连接、`Run`、`Abort` 与 RAW。 | **SKIPPED-POLICY**；即使是假实现，仍按“不得执行 Run/Abort”字面边界跳过。 |
| `device_actions` | 纯状态可用性映射。 | SAFE；运行。 |
| `image_quality_evaluator` | 对内存和测试图像做质量计算。 | SAFE；运行。 |
| `mock_workflow` | 在临时目录验证 Mock FSM。 | SAFE；运行。 |
| `mock_result_package` | 在临时目录写入并校验 Mock 包。 | SAFE；运行。 |
| `main_window` | 仅一个测试以 `FAKE_MRI_SDK_PATH` 调 `loadSdkAndConnect`；其余为 offscreen Mock/HOLD UI。最终提交在 `initTestCase` 将结果根全局固定为 `QTemporaryDir`。 | SAFE-FAKE；运行。 |
| `eggcontroller_process` | `QProcess` 只启动编译出的 `fake_eggcontroller_proxy.exe`。 | SAFE-FAKE；运行。 |
| `eggcontroller_proxy_py` | 用 `FakeUi` 调 proxy `run_once`；不导入真实 `MainWindow_V3`。 | SAFE-FAKE；运行。 |
| `sdk_verify_fake` | 命令显式带 verifier `--scan`。 | **BLOCKED**；跳过。 |
| `gui_auto_connect_fake` | 命令显式带 `--auto-connect`。 | **BLOCKED**；跳过。 |
| `gui_automation_mode_fake` | 以 offscreen、fake 路径启动测试 GUI 3 秒；current `configureEggController` 只保存配置并显示 HOLD，不启动 QProcess。 | SAFE-FAKE；运行。 |

精确白名单为：

```text
^(mri_sdk_loader|device_actions|image_quality_evaluator|mock_workflow|mock_result_package|main_window|eggcontroller_process|eggcontroller_proxy_py|gui_automation_mode_fake)$
```

三项被禁止的测试仍注册在 CTest；因此裸 `ctest` 不是本阶段允许的复验命令。

## 3. 表征测试和旧断言探针

### 3.1 当前可执行表征

`7aa2fc8` 新增三项不改生产代码的当前行为表征：

1. `defaultSdkStateIsUnloadedAndRealButtonsDisabled`：初始会话为 `Unloaded`，五个真实动作按钮禁用。
2. `configureEggControllerKeepsProxyUnreachableAndReportsHold`：配置一个不存在的代理路径后，QProcess 未运行、状态显示 HOLD、真实模式与 Run 仍禁用。
3. `holdHandlersLeaveMockNavigationAndStatusVisible`：直接调用五个当前 handler 后，会话仍为 `Unloaded`、真实预检未通过，Mock 01→02 导航仍可见。

这些断言只锁定上述有限观察点；它们不证明所有未来可达性变化都会使测试失败，也不证明产品交互完整。

`70c5607` 继续做测试范围修正：

- 用测试级临时 `SCENARIO_NMR_MOCK_RESULT_ROOT` 隔离所有 `MainWindow` 构造和 DRY_RUN 检查；
- 明确断言 DRY_RUN 前后隔离的 Mock 结果根条目不变；该断言不覆盖休眠 `DeviceBridge::dryRunScene` 可能写入的 application-dir `dry_run_params`，current 无该 caller 的结论仍依赖静态调用搜索；
- 删除 `#if 0` 包住的旧 automation 测试文本；
- 删除 `return` 之后永远不会执行的约 180 行旧交互断言，并将该用例如实命名为 `visibleWorkflowControlsHaveStableObjectNames`。

### 3.2 仓库外 RED 探针

为避免把死代码直接当作当前合同，在 `C:\tmp\agent-mri-stage2-probe-return` 的隔离副本中只解除旧测试的提前 `return`，随后逐步绕开已定位断言以寻找下一个分歧；未把这些 probe 修改带回仓库。结果依次为 RED：

| 日志 | SHA-256 | 暴露的当前差异 |
|---|---|---|
| `probe-return-direct.stdout.log` | `F06899639B92F8AC98F366730B18CC01B6B0FE0F2EB741430451FA1C8C2C6DC0` | 禁用的“版本保存”没有旧断言要求的反馈。 |
| `probe-return-direct2.stdout.log` | `1923A1562FA7D4B8F9871893CF93CD9D31A5572C8C436CCA0DCE6B00F70C350A` | 导航已使协议草稿失效，确认框后 `MockAcquireButton` 仍禁用。 |
| `probe-return-direct3.stdout.log` | `F915F2641304C5D33E14EE6E4127BFC3C85B8F9E56A65EF6A75D56F18D51AFB3` | 仅强制页号到 10、缺少工作流状态时，处理按钮不会进入 11。 |
| `probe-return-direct4.stdout.log` | `2C032CF5F2DAD106D604074573FF90FEF50DE0F4B889D3EFED351408BF9EA83D` | 禁用的结果目录动作没有旧反馈。 |
| `probe-return-direct5.stdout.log` | `5E8F700DF31ACCFBBC7F0C0D30445A518932CF6F9B9B29F09D2B880001F4431F` | 重复最小化后仍在结果目录旧反馈处失败。 |

结论：旧块确实是未执行覆盖洞，但其断言也已和 current 状态模型冲突；删除死文本是测试诚实性修正，不是产品行为 GREEN，也不能称为按钮问题已修复。

## 4. 仓库外新鲜构建

所有构建物和原始日志均在 `C:\tmp\agent-mri-stage2-20260802`。工具固定为 UCRT64 CMake、Ninja、G++；未使用现有 `client/build-*-ascii`。

### 4.1 项目 A：exact `80b5d853761ead14a8728c0be9c807fda3f75004`

- `git archive`：757,216 bytes，SHA-256 `2BD5DFB8010D7EEBA33A7EAD46E296B16BA546466AB8BCD67D69FE46DC7150F3`。
- 第一次未固定编译器，环境选中 Cygwin 并在 AUTOMOC 失败；日志 `D9901088933139B6DEDD13B6DC0B5DFB39CCADD1FA812A8E5CB8C52673B5A0BA`。这证明环境默认工具链不可靠，不是源码缺陷。
- 在全新 `build-a-debug-ucrt` 显式固定 UCRT64 后 configure/build 成功；日志分别为 `A678E8D3FEA5BFBDA95B789BF98FC23CB2E0EAF8AC06F6A0B1AE4AEDB48761D9`、`D52DDFA7DC61342805EF978DC9AB3CAC52B9911DFB0A32B12856648E822B62ED`。
- `ctest -N` 为 0 tests；日志 `C301A5096ECCA2118C22378A4564813A974BB6A4CBBC1B54DE98F534BB2DF091`。
- Debug EXE 未启动：5,090,335 bytes，SHA-256 `A93B2D2831B6BF94A24893D75E71DE20B65AA2E1BD980322BD7C466724556291`。

### 4.2 current：exact `70c5607454d661a9b325798b627c1412ed2869ba`

源码 worktree 为 `C:\tmp\agent-mri-stage2-current-70c5607`，Debug/Release 均在新目录完成 89 个 Ninja 步骤。构建 verifier 目标不等于执行 verifier。

| 配置 | configure | build | `ctest -N -V` | 白名单测试 | EXE（未人工启动） |
|---|---|---|---|---|---|
| Debug | `2BE26D2CEFD68DF8040264A461A5C5AB73BF1AF8F3D8CA3CF1A15899ABFD1948` | `296C214F819E77C612C7BFFC763ECFD9BCC5AB601C896DD8B7AE4E476E759734` | `A5EEA4998A5C59BAAECAED7C7757003945F5BE66D1D7E96CF9F45F1A036D8C21` | 9/9，22.55 s；`3EF7581A6027097BED1C4255C766F3A8D46367B6E96252761D6EB430481D190F` | 19,055,227 bytes；`37FD281B2424C02591305C9E944BA67271BEE63D412F2577F983CA02C41EA127` |
| Release | `03BF51FF0862BBE740A5EE03E7CBEE52E82C3F8D089DEC90B35BE083202433E4` | `55527A798828D230101513231E937A88CCEC089C0D45749166C730657785B911` | `D5D12AF488AF76BFC7981EC600AC0A9D8499546BB40CE9BA03C911783D646959` | 9/9，21.93 s；`D212F6E375E0033F00000CBC440960B862A1106E53CC1841579353CC6E0BC60B` | 6,811,293 bytes；`7E873A01058E3B2395804D5754CD19730B66B78B263DB36D22D7ACABD03FFEF8` |

两个 EXE 的静态字节都包含 exact commit `70c5607454d661a9b325798b627c1412ed2869ba`。二者都未作为用户界面人工启动；只有白名单中的 `gui_automation_mode_fake` 以 `QT_QPA_PLATFORM=offscreen`、fake 参数运行约 3 秒。

“9/9”指九个 CTest 进程通过，不等于 `main_window` 内每个 Qt 子用例都执行：`captureLocalizationPlanningEvidenceWhenRequested` 与 `captureInteractionQaScreensWhenRequested` 因本阶段未设置截图输出环境变量而按设计 `QSKIP`。这两项是证据截图采集器，不影响已运行的其他表征断言，但不能计为已覆盖截图流程。

## 5. 文件系统隔离审计

在首轮 `7aa2fc8` 测试中，测试导航到第 8 页并触发运行确认 checkbox 的门禁重算；该路径调用 `mockOutputRootWritable()`，在默认结果根执行 auto-remove 可写性探针。虽然未留下新文件或结果包，但既存 `C:\Users\Administrator\Documents\ScenarioNmr\MockRuns` 的目录 mtime 被更新为 `2026-08-02T16:24:57.7424432+08:00`；其中仍只有原有两个目录。本阶段没有删除、改写或尝试伪造恢复这些用户目录。窗口构造本身只选择结果根，不执行该探针。

`70c5607` 将全套 `main_window` 测试结果根固定到测试级 `QTemporaryDir`。最终 Debug/Release 白名单运行前后的上述用户根 mtime 和条目均未变化，证明最终复验已封闭该副作用。首轮痕迹仍如实保留为证据限制。

## 6. 本阶段没有证明什么

- 没有加载 D32 真实 DLL、连接设备、执行 `--auto-connect`、verifier `--scan` 或真实/fake `device_bridge` Run/Abort 测试。
- 没有启动面向用户的 Qt 窗口、MRIScanner、真实 eggcontrollerV2 或真实代理入口。
- A 构建成功只证明当前本机显式 UCRT64 可编译；A 没有自动测试。
- current 白名单通过只证明所列 fake/Mock/HOLD 行为，不证明厂家 ABI、设备状态语义、扫描安全或 RAW→PNG 同次关系。
- 详细命令、绝对路径和完整哈希见 [build-test-evidence.json](evidence-stage2/build-test-evidence.json)。
