# Requirement

## User Request

将中间顶部红框区域从混合信息胶囊改为“操作链”，把链路节点显示清楚。

## Success Criteria

- 中间主区标题从“实验链”改为“操作链”。
- 顶部链路以明确节点呈现，每个节点包含步骤号、节点名称和模板内具体内容。
- 节点顺序体现从模板确认到预检、定位、采集、重建、质控交接的工作流。
- 文本不再挤在同一行；窗口宽度不足时允许横向滚动，避免重叠。

## Out Of Scope

- 不调整 SDK 控制逻辑。
- 不修改参数预设。
- 不解除真实 `Run()` 的 `HOLD`。
