# Handoff

## Completed

- Reworked the right pane into `QC overview + tabs`.
- Added tabs for `参数预设`, `SDK 诊断`, and `日志`.
- Reduced metric value font size and enabled wrapping.
- Increased right pane splitter allocation.
- Synced changes to `/Users/martin/Project/nuclear_system`.
- Verified build and offscreen startup.

## Run

```bash
cd /Users/martin/Project/nuclear_system/client
cmake --build build -j
./build/scenario_nmr_client
```
