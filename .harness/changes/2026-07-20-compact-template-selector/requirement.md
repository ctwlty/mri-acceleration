# Requirement

## User Request

左侧任务选择区域存在大量空白，需要缩减区域；同时需要考虑未来非常多场景和模板时该如何选择。

## Success Criteria

- 缩减左侧选择区域高度和宽度占用。
- 避免少量模板时出现大面积空白。
- 保留 `一级场景 + 检测对象 -> 推荐模板` 的业务逻辑。
- 支持通过关键词搜索模板、对象、协议或输出类型。
- 不修改 SDK、参数预设、`Run HOLD` 边界。

## Out Of Scope

- 不新增服务端模板库。
- 不做用户权限、收藏、最近使用持久化。
- 不改变七类科研任务数据。
