# Affected Files

- `client/src/app/MainWindow.h`
- `client/src/app/MainWindow.cpp`
- `client/resources/app.qss`

## Notes

本次将左侧选择组件从三个 `QListWidget` 改为两个 `QComboBox`、一个 `QLineEdit` 和一个模板结果 `QListWidget`。
