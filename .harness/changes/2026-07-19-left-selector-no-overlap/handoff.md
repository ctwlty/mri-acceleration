# Handoff

## Completed

- 删除左侧模板列表下方的摘要表格。
- 删除左侧说明文字。
- 左侧变为纯选择区：任务标题、模板列表、操作按钮。
- 列表最小高度从 430 降到 320，避免小窗口下挤压。

## Verification

- `cmake --build build -j` 通过。

## Follow-ups

- 后续可以在左侧增加搜索框，但不要增加长文本详情。
- 详细信息继续放中部实验链和右侧参数/QC 区。
