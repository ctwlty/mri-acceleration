# Test Report

| Gate | Command | Result | Evidence |
| --- | --- | --- | --- |
| build | `cmake --build build -j` | Pass | `scenario_nmr_client` built successfully |

## Skipped Gates

- Real SDK parameter write: 本轮只显示开发预设参数，不写入真实 SDK。
