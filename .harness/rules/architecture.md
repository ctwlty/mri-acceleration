# Architecture Rules

- Preserve the repository's existing layering and naming patterns.
- Keep business logic out of thin route/controller layers when the codebase has services/use cases.
- Prefer existing helpers, models, clients, and test patterns before adding abstractions.
- Document any cross-layer dependency introduced by a change.
