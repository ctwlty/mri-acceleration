# Security Rules

- Never commit real secrets, API keys, passwords, refresh tokens, or private credentials.
- Never log full secrets. Mask sensitive values in logs, errors, exports, screenshots, and reports.
- Auth, permission, billing, and data-export changes require targeted tests or a documented manual check.
- Prefer dry-run mode for destructive or production-affecting tools.
